import { StraightPoolPlayer } from './straight-pool-player';

describe('StraightPoolPlayer', () => {
  it('should create an instance', () => {
    expect(new StraightPoolPlayer()).toBeTruthy();
  });
});
